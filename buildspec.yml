version: 0.2
run-as: root

phases:
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - npm uninstall -g angular-cli @angular/cli
      - npm cache clean --force
      - npm install -g @angular/cli@11.2.11
      - npm install --save-dev @angular/cli@11.2.11
      - rm package-lock.json
      - npm install
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - REPOSITORY_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...          
      - docker build -t team-magenta-group-two .
      - docker tag team-magenta-group-two:latest $AWS_ACCOUNT_ID.dkr.ecr.eu-west-1.amazonaws.com/team-magenta-group-two:latest     
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
      - echo Writing image definitions file...
      - printf '[{"name":"%s","imageUri":"%s"}]' $CONTAINER_NAME $REPOSITORY_URI:$IMAGE_TAG > imagedefinitions.json
artifacts:
    files: imagedefinitions.json